import { SalaryRangePipePipe } from './salary-range-pipe.pipe';

describe('SalaryRangePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SalaryRangePipePipe();
    expect(pipe).toBeTruthy();
  });
});
